# terminal-multiplexer-go

Super simple terminal multiplexer in Go.

It blends ideas from `tmux` and a Niri-style scrollable window manager:
- terminal-first workflows like tmux sessions/panes
- smooth, scrollable workspace navigation inspired by Niri

This is just a proof of concept and is not ready for production use.

Window focus changes only on click (not on hover).
